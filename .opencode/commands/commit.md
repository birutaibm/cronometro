---
description: 'Commit the project for development or production'
agent: builder
---

Commit the Cronômetro project.

Check your current banch in the repository. If it is `main`, checkout to a new branch before proceed.

List all files to be commited and ensure that none of them must be ignored by git.

Commit changes with a relevant message.

Publish this commit on github via MCP.

Open a new PR on github via MCP from published bransh to `main` branch.
